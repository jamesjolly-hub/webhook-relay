{
  "name": "webhook-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "kv:create": "wrangler kv namespace create RELAY_STORE",
    "kv:create:preview": "wrangler kv namespace create RELAY_STORE --preview"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.0",
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
